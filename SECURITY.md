# OpenKeyMouse 安全政策

## 安全边界

- 以 Vimium v2.4.2、提交 eb737ab 为代码基线；
- 保持 Manifest V3、最少权限和现有 Firefox 兼容路径；
- 不使用 eval、new Function、远程脚本或动态代码下载；
- 不信任网页脚本通过 window.postMessage 触发高权限操作；
- 不把 URL、页面内容、输入或鼠标轨迹写入日志；
- 配置导入必须先解析、校验和确认，不执行导入文件中的 HTML 或脚本；
- 新增权限、运行时依赖或网络行为必须先有 ADR。

## Phase 0 已执行的安全收敛

本阶段没有新增权限、运行时依赖、网络请求或内容脚本。上游仅用于版本升级提示的 notifications
权限和品牌绑定升级通知已移除；键盘导航、链接提示、Vomnibar 和现有测试路径不做功能重构。

## 漏洞报告

在公开仓库和安全联系渠道建立前，请不要把真实凭证、个人资料、Cookie、Token 或完整浏览数据提交到
issue。发布仓库后，维护者应提供公开 issue
和私下安全报告渠道，并在收到报告后记录受影响版本、复现步骤、修复提交和回归测试。

## 发布前安全门禁

发布前必须完成权限审计、网络调用审计、CSP 检查、第三方许可证核对、干净 checkout
构建和跨浏览器验证；未完成的检查不得标记为发布通过。
