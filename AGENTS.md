# 浏览器工具箱协作规则

## 命名约定

- 正式对外产品名已确定为“浏览器工具箱”；图标归属、商标可用性和法律结论仍须单独检索确认。
- `BrowserToolbox` 是当前唯一的技术标识，统一用于命令名、消息协议、存储键和自动化测试；旧技术标识只允许出现在兼容迁移层和历史记录中。
- “浏览器工具箱”是产品范围，不等于鼠标手势扩展；鼠标手势、超级拖拽、滚轮和摇杆只是其中的输入模块。

## 事实源和阶段门禁

- 设计文档 BrowserToolbox_Codex_可执行开发设计文档.md 是本项目施工规范；其中的对外定位以本轮命名约定为准。
- Phase 0 基线已经记录并通过；当前 checkout 已进入功能实现后的兼容性、无障碍和预发布门禁收口阶段。
- 当前代码基线固定为 Vimium v2.4.2、提交 eb737abd；这不是浏览器工具箱的发布版本号。当前共享工作区物理目录为 `browser-toolbox`，与运行时技术标识保持一致。
- 当前 checkout、实际测试输出和运行态优先于历史报告；不得把未运行的测试写成通过。

## 开源与隐私

- 官方版本永久免费、无广告、无账户、无订阅、无付费功能和无商业版。
- 不加入遥测，不上传 URL、历史、页面内容、键盘输入、选中文字、搜索词或鼠标轨迹。
- 不加载远程代码，不使用 eval 或 new Function。
- 不解包、复制或改写 CrxMouse 的闭源代码、图片、图标、文案和设置界面。
- 新增权限、依赖或网络行为前必须先记录 ADR；默认采用最少权限、最少依赖和无网络依赖。

## 修改规范

- 保留 Vimium 文件的原始 MIT 版权与许可说明；全新功能代码再按设计文档使用 GPL-3.0-or-later。
- 不一次性重构上游代码，不删除既有键盘行为。
- 所有新增代码注释使用中文；状态机注释应解释状态转换，不重复代码表面含义。
- 修改后必须运行真实测试并在 docs/codex-progress.md 追加命令、结果、风险和提交信息。
- 不提交凭证、个人资料、Cookie、Token、浏览数据或测试产生的二进制缓存。

## Git 与浏览器测试规范

- 所有 Git 提交描述一律使用英文，包括提交标题和提交正文。
- 需要浏览器测试时，默认使用 Google Chrome for Testing 应用；只有在验收目标明确要求其他浏览器时才切换。
- Chrome for Testing 的自动化结果不得写成日常用户态 Chrome、人工验收或其他浏览器的通过证据。

## 当前阶段边界

统一命令层、鼠标轨迹、超级拖拽、滚轮、摇杆、标签列表、自定义指针、站点规则、配置 schema、可视设置页和
运行时审计已经进入当前 checkout；后续修改必须保持现有键盘行为，并补充真实测试和验收记录。

当前仍禁止：把没有实际证据的 Windows、Edge、Linux Chrome Stable、macOS Chrome Stable 人工矩阵标记为通过；
把自动化隔离结果写成认证态站点、屏幕阅读器或完整 Vimium 手工回归已完成；引入商业化、遥测、远程代码、未经
ADR 批准的权限/依赖/网络行为，或复制 CrxMouse 闭源代码、资产、文案和界面。
